<div class="signup" id="signup">
	<form method="post" action="https://formspree.io/f/xzzplrdg">
		<label for="email">Legg igjen e-postadressen din:</label>
		<input name="email" type="email" value="" placeholder="navn@universitet.no" />
		<button type="submit">Kontakt meg</button>
	</form>
	<div class="overlay"></div>
</div>
<script is:inline>
	window.addEventListener("hashchange", () => {
		if (window.location.hash === "#signup") {
			document.querySelector("input[name=email]").focus();
		}
	});
</script>
<style>
	.signup {
		padding: 1rem 0.5rem;

		@media screen and (min-width: 720px) {
			padding: 2rem 1rem;
		}

		display: flex;
	}
	.signup:focus-within .overlay {
		opacity: .5;
	}
	.overlay {
		pointer-events: none;
		background: rgb(var(--accent));
		position: fixed;
		left: 0;
		top: 0;
		width: 100%;
		height: 100%;
		z-index: 1;
		opacity: 0;
		transition: opacity .15s ease;
	}
	form {
		position: relative;
		z-index: 2;
		background: white;
		margin: auto;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
		padding: 2rem 1rem;
		border: 2px solid rgb(var(--accent));
		border-radius: 2rem;
	}
	label {
		font-weight: bold;
		font-size: 1.5rem;
		color: rgb(var(--accent));
		
		@media screen and (min-width: 720px) {
			font-size: 2rem;
		}
	}
    form input {
		padding: 0.75rem 1rem;
		font-size: 1.5rem;
		border-radius: .5rem;
		border: 1px solid #333;
		&:focus {
			outline: 3px solid rgb(var(--accent));
		}
	}
	form button[type=submit] {
		font-size: 1.2rem;
		padding: 1.25rem 1.75rem;
		background: rgb(var(--accent));
		color: white;
		border: none;
		border-radius: 4rem;
		transition: outline ease .15s;
		cursor: pointer;
		&:hover, &:active {
			outline: 3px solid rgb(var(--accent));
		}
	}
</style>